# Security Policy

## 支持范围

安全修复面向默认分支上的当前源码。历史快照、个人分支和第三方依赖自身的问题不单独维护；第三方依赖漏洞仍可报告，以便评估本项目是否受影响。

## 私下报告漏洞

请优先使用 GitHub 的 [Private Vulnerability Reporting](https://github.com/Lucoshin/vibe-foundry/security/advisories/new) 提交安全报告。不要先创建公开 Issue，也不要在报告中附上真实访问凭据、用户数据或未脱敏的本地资产库。

报告中建议包含：

- 受影响的版本、提交或功能。
- 可重复的最小步骤和预期影响。
- 已做脱敏的日志、示例或概念验证。
- 你认为可行的缓解方式（如有）。

维护者会在 GitHub 私密报告中进行确认、复现和修复协调。公开时间应在修复可用后共同确定；在此之前请避免披露可被直接利用的细节。

## 信任边界

普通 `distill` 不自动执行源项目脚本，当前公开 CLI 也不提供这项执行入口。仓库内的源会话能力只有在调用方显式提供 `sourceScript` 时才运行源项目中准确存在的 package script，并且只接受本机回环 `sourceUrl`。package script 能执行任意代码，因此只应对可信项目明确授权运行。子进程环境使用固定白名单，不会自动继承宿主的任意变量或令牌，但该白名单不是操作系统沙箱：脚本仍拥有当前操作系统用户的文件系统和网络权限。本地资产库仍可能包含绝对路径和项目分析结果，不应提交或直接分享。

组件预览只应执行可信源码：预览页面与 Web Asset Browser 同源，可访问父窗口及同源的其他 API；`networkPolicy: "block-external"` 只用于确定性地阻断外部网络请求，不是安全沙箱。不要用 Web Asset Browser 预览不可信项目，也不要把服务暴露给不可信网络。
