# 放大预览复用已加载实例

确认问题：缩略预览已加载，但工作区再次创建 iframe，重复执行组件初始化。

使用已确认当前浏览器支持的 DOM moveBefore 保留 iframe 文档状态，将缩略实例移入工作区，返回时移回原卡片；不修改 src、不重新创建已存在实例。直接打开尚未显示缩略图的组件仍创建初始 iframe。刷新按钮保留显式重载。

验证 iframe 身份、组件状态和放大前后网络请求；运行交互测试及全量 npm test。清理重复创建路径，保留原有画布与动作变更处理。

## 验证结果

- 真实 Editor：放大前后同一 iframe、同一 contentDocument，新增资源请求 0；返回列表继续保持同一文档。
- npm run build 通过；全量串行 node --test --test-concurrency=1 "tests/**/*.test.mjs"：411/411 通过。并行首次测试受负载影响有超时，串行完成验证。
- 已清理放大时重复创建 iframe 路径；直接初次预览和用户显式刷新仍保留，原因分别为尚无实例和主动重载。
