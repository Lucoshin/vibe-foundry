# 书籍知识资产验证样例

《雾港》是本仓库为验证工作流编写的原创短篇。它故意包含不同称呼、同名不同人、人物转变、未经证实的传言与可作隐喻解读的意象。

`analysis.json` 是对本样例的预先阅读分析，用于校验准备/导入、来源定位和输出。它不是模型性能测评数据，也不能代表整本长书的提取准确率。

构建后运行 `node scripts/verify-book-knowledge.mjs`；带 `--output output/book-knowledge` 可保留阅读任务、资产与关系图供浏览。本样例不依赖模型 API。
